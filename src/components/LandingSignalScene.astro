<section class="relative isolate overflow-hidden rounded-[2rem] border border-white/8 bg-[radial-gradient(circle_at_top,rgba(0,229,160,0.14),transparent_38%),linear-gradient(180deg,rgba(255,255,255,0.04),rgba(255,255,255,0.01))] px-6 py-10 md:px-10 md:py-14">
  <div class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_18%_18%,rgba(59,130,246,0.16),transparent_24%),radial-gradient(circle_at_82%_16%,rgba(0,229,160,0.14),transparent_22%),radial-gradient(circle_at_50%_82%,rgba(255,255,255,0.05),transparent_28%)]"></div>
  <div class="grid items-center gap-10 lg:grid-cols-[0.94fr_1.06fr]">
    <div class="relative z-10">
      <div class="font-mono text-[11px] uppercase tracking-[0.12em] text-accent">SwimSentry</div>
      <h1 class="mt-4 max-w-[12ch] text-5xl font-semibold tracking-tight text-text-primary md:text-6xl">
        One clear sensor slice, instead of a whole crowded system.
      </h1>
      <p class="mt-5 max-w-[58ch] text-base leading-7 text-text-secondary">
        This view focuses on a single SwimSentry sensor body and its internal layers: weather shell, optics, compute, and power. It introduces the product without making the user decode a complex scene first.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a href="/build" class="rounded-full bg-accent px-5 py-3 text-sm font-semibold text-bg-deep">
          Start a build
        </a>
        <a href="/personas" class="rounded-full border border-white/10 bg-white/5 px-5 py-3 text-sm font-medium text-text-primary">
          Open guided flow
        </a>
        <a href="/bom/compare" class="rounded-full border border-white/10 bg-white/5 px-5 py-3 text-sm font-medium text-text-primary">
          Compare systems
        </a>
      </div>
    </div>

    <div class="slice-stage relative z-10 h-[34rem] overflow-hidden rounded-[1.8rem] border border-white/8 bg-[linear-gradient(180deg,rgba(8,10,14,0.9),rgba(9,11,15,0.98))]">
      <div class="slice-atmosphere"></div>
      <div class="slice-floor"></div>

      <div class="device-slice">
        <div class="slice-layer shell-layer">
          <div class="layer-card">
            <div class="layer-chip">Outer shell</div>
            <div class="layer-shape shell-shape"></div>
            <div class="layer-copy">IP-rated enclosure and faceplate</div>
          </div>
        </div>

        <div class="slice-layer optics-layer">
          <div class="layer-card">
            <div class="layer-chip">Optics</div>
            <div class="layer-shape optics-shape">
              <span class="lens-ring lens-top"></span>
              <span class="lens-ring lens-bottom"></span>
            </div>
            <div class="layer-copy">Visible camera plus close-range sensing</div>
          </div>
        </div>

        <div class="slice-layer core-layer">
          <div class="layer-card">
            <div class="layer-chip">Compute</div>
            <div class="layer-shape core-shape">
              <span class="trace trace-a"></span>
              <span class="trace trace-b"></span>
              <span class="trace trace-c"></span>
            </div>
            <div class="layer-copy">Processing, radio, and local logic</div>
          </div>
        </div>

        <div class="slice-layer power-layer">
          <div class="layer-card">
            <div class="layer-chip">Power</div>
            <div class="layer-shape power-shape"></div>
            <div class="layer-copy">Battery and power regulation base</div>
          </div>
        </div>
      </div>

      <div class="slice-callout callout-top">
        <div class="callout-line"></div>
        <div class="callout-box">
          <div class="callout-title">Exploded slice</div>
          <div class="callout-copy">Each layer is separated just enough to show how the hardware stacks.</div>
        </div>
      </div>

      <div class="slice-callout callout-side">
        <div class="callout-line"></div>
        <div class="callout-box">
          <div class="callout-title">Single device focus</div>
          <div class="callout-copy">The page now teaches one product clearly before moving into builds and BOMs.</div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .slice-stage {
    perspective: 1400px;
    transform-style: preserve-3d;
  }

  .slice-atmosphere,
  .slice-floor {
    position: absolute;
    inset: 0;
  }

  .slice-atmosphere {
    background:
      radial-gradient(circle at 25% 20%, rgba(59, 130, 246, 0.18), transparent 26%),
      radial-gradient(circle at 74% 18%, rgba(0, 229, 160, 0.14), transparent 22%),
      radial-gradient(circle at 52% 58%, rgba(255, 255, 255, 0.05), transparent 30%);
  }

  .slice-floor {
    inset: auto 12% 8% 12%;
    height: 30%;
    border-radius: 28px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.01)),
      linear-gradient(180deg, rgba(11, 15, 21, 0.78), rgba(7, 9, 12, 0.96));
    border: 1px solid rgba(255, 255, 255, 0.05);
    transform: rotateX(76deg) translateZ(-14px);
    transform-origin: bottom center;
    box-shadow: 0 50px 90px rgba(0, 0, 0, 0.4);
  }

  .device-slice {
    position: absolute;
    left: 50%;
    top: 12%;
    width: 15rem;
    transform: translateX(-50%);
  }

  .slice-layer {
    position: relative;
    animation: layer-drift 6.5s ease-in-out infinite;
  }

  .shell-layer { z-index: 4; animation-delay: -0.2s; }
  .optics-layer { z-index: 3; margin-top: 1.1rem; animation-delay: -1.2s; }
  .core-layer { z-index: 2; margin-top: 1.1rem; animation-delay: -2.2s; }
  .power-layer { z-index: 1; margin-top: 1.1rem; animation-delay: -3.2s; }

  .layer-card {
    position: relative;
    border-radius: 1.8rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.14), rgba(255, 255, 255, 0.04)),
      linear-gradient(180deg, rgba(18, 23, 31, 0.92), rgba(8, 11, 16, 0.98));
    box-shadow:
      0 26px 50px rgba(0, 0, 0, 0.34),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);
    padding: 1rem 1rem 1.1rem;
    transform: rotateX(14deg) rotateY(-11deg);
  }

  .layer-chip {
    display: inline-flex;
    border-radius: 999px;
    border: 1px solid rgba(0, 229, 160, 0.2);
    background: rgba(0, 229, 160, 0.08);
    padding: 0.3rem 0.65rem;
    color: #00e5a0;
    font-family: 'DM Mono', monospace;
    font-size: 0.66rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .layer-shape {
    position: relative;
    margin: 0.95rem auto 0;
    border-radius: 1.25rem;
  }

  .shell-shape {
    width: 8rem;
    height: 5.4rem;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.02)),
      linear-gradient(180deg, rgba(22, 27, 35, 0.9), rgba(8, 10, 14, 1));
  }

  .optics-shape {
    width: 7rem;
    height: 4.6rem;
    background: linear-gradient(180deg, rgba(18, 24, 32, 0.9), rgba(8, 10, 14, 0.96));
  }

  .lens-ring {
    position: absolute;
    left: 50%;
    border-radius: 999px;
    transform: translateX(-50%);
    background:
      radial-gradient(circle at 35% 35%, rgba(255, 255, 255, 0.4), rgba(59, 130, 246, 0.34) 24%, rgba(7, 10, 14, 0.96) 56%, rgba(0, 0, 0, 0.98));
    box-shadow: 0 0 22px rgba(59, 130, 246, 0.15);
  }

  .lens-top {
    top: 0.5rem;
    width: 2.4rem;
    height: 2.4rem;
  }

  .lens-bottom {
    bottom: 0.45rem;
    width: 1.8rem;
    height: 1.8rem;
  }

  .core-shape {
    width: 7.8rem;
    height: 4.8rem;
    background:
      linear-gradient(180deg, rgba(15, 38, 33, 0.76), rgba(10, 19, 21, 0.96)),
      linear-gradient(180deg, rgba(18, 24, 32, 0.92), rgba(8, 10, 14, 0.98));
    overflow: hidden;
  }

  .trace {
    position: absolute;
    background: rgba(0, 229, 160, 0.72);
    box-shadow: 0 0 16px rgba(0, 229, 160, 0.18);
  }

  .trace-a {
    left: 1rem;
    top: 1rem;
    width: 4.6rem;
    height: 0.18rem;
  }

  .trace-b {
    left: 2.2rem;
    top: 2rem;
    width: 0.18rem;
    height: 1.6rem;
  }

  .trace-c {
    left: 2.2rem;
    bottom: 1rem;
    width: 3.5rem;
    height: 0.18rem;
  }

  .power-shape {
    width: 8.4rem;
    height: 3.1rem;
    background:
      linear-gradient(180deg, rgba(245, 158, 11, 0.14), rgba(245, 158, 11, 0.06)),
      linear-gradient(180deg, rgba(23, 20, 14, 0.92), rgba(10, 10, 12, 0.98));
  }

  .layer-copy {
    margin-top: 0.85rem;
    text-align: center;
    color: #8b8d98;
    font-size: 0.76rem;
    line-height: 1.45;
  }

  .slice-callout {
    position: absolute;
    z-index: 3;
    display: flex;
    gap: 0.8rem;
    align-items: flex-start;
  }

  .callout-top {
    top: 9%;
    right: 8%;
  }

  .callout-side {
    left: 8%;
    bottom: 14%;
  }

  .callout-line {
    width: 2.8rem;
    height: 1px;
    margin-top: 0.95rem;
    background: linear-gradient(90deg, rgba(0, 229, 160, 0.18), rgba(0, 229, 160, 0.82));
  }

  .callout-box {
    max-width: 11rem;
    border-radius: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    background: rgba(255, 255, 255, 0.04);
    padding: 0.85rem 0.95rem;
    backdrop-filter: blur(10px);
  }

  .callout-title {
    color: #e8e9ed;
    font-size: 0.82rem;
    font-weight: 600;
  }

  .callout-copy {
    margin-top: 0.28rem;
    color: #8b8d98;
    font-size: 0.72rem;
    line-height: 1.45;
  }

  @keyframes layer-drift {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }

  @media (max-width: 1023px) {
    .slice-stage {
      height: 30rem;
    }

    .device-slice {
      top: 11%;
      transform: translateX(-50%) scale(0.92);
    }

    .slice-callout {
      display: none;
    }
  }
</style>
